/* Legacy top-level parser translation unit kept for compatibility.
   Implementation now lives in `module1/parser.c`. This file intentionally
   contains no definitions to avoid duplicate symbols.
*/
#include "module1/parser.h"

