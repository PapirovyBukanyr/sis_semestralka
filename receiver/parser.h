/* Legacy top-level parser header kept for compatibility.
   Prefer `module1/parser.h` (parser moved into module1 because it deals with input).
*/
#ifndef PARSER_H
#define PARSER_H

#include "module1/parser.h"

#endif /* PARSER_H */
